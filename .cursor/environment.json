{
  "name": "3Dgame Unity+Blender",
  "install": "bash scripts/setup/install-blender-linux.sh && git lfs install && bash scripts/setup/verify-env.sh || true",
  "start": "echo 'Unity Editor runs on your local PC (Windows recommended). Blender CLI available via ~/bin/blender'"
}
